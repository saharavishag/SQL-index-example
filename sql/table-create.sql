CREATE TABLE `clothing-db`.clothing_stock (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    category VARCHAR(255),
    product_type VARCHAR(255),
    brand VARCHAR(255),
    size VARCHAR(255),
    stock INT
);
